import { AccountStep } from "@/components/auth/OnboardingSteps";

export const metadata = { title: "Account · Onboarding" };

export default function OnboardingAccountPage() {
  return <AccountStep />;
}

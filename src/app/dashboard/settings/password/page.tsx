import { PasswordSettingsForm } from "@/components/dashboard/SettingsForms";

export const metadata = { title: "Password · Settings" };

export default function SettingsPasswordPage() {
  return <PasswordSettingsForm />;
}
